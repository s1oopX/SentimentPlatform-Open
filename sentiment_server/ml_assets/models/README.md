# Model Assets

No model weights are stored in this public repository.

Place local model artifacts here when running or training SentimentPlatform. For example:

```text
models/bert/
├── config.json
├── model.safetensors
├── tokenizer_config.json
├── tokenizer.json
└── vocab.txt
```

These files are intentionally ignored by Git.
