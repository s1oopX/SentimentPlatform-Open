# Dataset Assets

No datasets are stored in this public repository.

Use this directory for local development and training data only. Do not commit original datasets, generated Arrow files, split outputs, or private data.

The `stopwords/` directory is kept because it contains small redistributable stopword lists used by preprocessing tests and utilities.
